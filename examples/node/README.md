A simple example of how to use Hypher in Node.js. First install the dependencies using NPM:

    npm install

You can then run the example with:

    node hyphenate.js [word] ...

Where `[word]` is one or more words. 
